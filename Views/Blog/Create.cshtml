@addTagHelper "*, Microsoft.AspNetCore.Mvc.TagHelpers"
@*Microsoft.AspNetCore.Mvc.TagHelpers.FormTagHelper*@
@model ExploreCalifornia.Models.Post
@{
    Layout = "_layout";
}
<h1>Create Blog Post</h1>
<fieldset>
    <!-- at using (Html.BeginForm())-->
    <form asp-action="Create" asp-controller="Blog">
        <!--{-->
        <!-- at Html.ValidationSummary()-->
        <div asp-validation-summary="ModelOnly" class="input-validation-error"></div>
        <!-- validation summary types All-all errors, ModelOnly-high level errors not specifc to any property, None-supress all errors -->
        <p>
            <label asp-for="Title"></label>
            <input asp-for="Title" class="form-control" placeholder="Blog post Title"/>
            <span asp-validation-for="Title"></span>
            @*@Html.LabelFor(x => x.Title)
            @Html.EditorFor(x => x.Title)
            @Html.ValidationMessageFor(x => x.Title)*@
        </p>
        <p>
            <!--ctrl + alt instead of just alt for mutli line select?-->
            <label asp-for="Body"></label>
            <textarea asp-for="Body" class="form-control" placeholder="Blog post Content"></textarea>
            <span asp-validation-for="Body"></span>
            @*@Html.LabelFor(x => x.Body)
        @Html.EditorFor(x => x.Body) <!--TextArea-->
        @Html.ValidationMessageFor(x => x.Body)*@
        </p>
        <p>
            <button type="submit">Create Post</button>
        </p>
    </form>
    <!--}-->

</fieldset>
<!--css for the validatinmessages in wwwroot/css/main.css -> line 224-240 -->
<!--http://localhost:53934/blog/create-->